"""
fusion_node.py — Sensor fusion: combine LiDAR geometry + camera semantics.

YOUR TASK:
  1. Subscribe to traversability grid + semantic labels
  2. Combine geometry score + semantic score into fused grid
  3. Publish fused BEV traversability
  4. Compare geometry-only vs fused (save comparison figures)

PDF reference: Part 5 (Day 5)
"""
